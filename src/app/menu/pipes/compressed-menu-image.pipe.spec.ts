import { CompressedMenuImagePipe } from './compressed-menu-image.pipe';

describe('CompressedMenuImagePipe', () => {
    it('create an instance', () => {
        const pipe = new CompressedMenuImagePipe();
        expect(pipe).toBeTruthy();
    });
});
